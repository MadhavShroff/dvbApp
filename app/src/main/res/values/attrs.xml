<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="MainParamsView" parent="Theme.MaterialComponents.NoActionBar">
        <attr name="maxValue" format="string" />
        <attr name="minValue" format="string" />
        <attr name="minText" format="string" />
        <attr name="maxText" format="string" />
        <attr name="value" format="string" />
        <attr name="label" format="string" />
        <attr name="unit" format="string" />
    </declare-styleable>

    <declare-styleable name="ControlRow" parent="Theme.MaterialComponents.NoActionBar">
        <attr name="controlLabel" format="string" />
        <attr name="controlUnit" format="string" />
        <attr name="current" format="string" />
    </declare-styleable>

</resources>